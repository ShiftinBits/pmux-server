{
  "name": "@pocketmux/server",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker + Durable Object signaling server for PocketMux",
  "license": "MIT",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run"
  },
  "dependencies": {
    "@pocketmux/shared": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260302.0",
    "typescript": "^5.7.0",
    "vitest": "^4.0.18",
    "wrangler": "^4.68.0"
  }
}
