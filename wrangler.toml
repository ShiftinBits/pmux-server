name = "pmux-signaling"
main = "src/worker.ts"
compatibility_date = "2024-12-01"

[durable_objects]
bindings = [
  { name = "SIGNALING", class_name = "SignalingDO" }
]

[[migrations]]
tag = "v1"
new_sqlite_classes = ["SignalingDO"]

# Environment secrets:
# - TURN_TOKEN_ID
# - TURN_API_TOKEN
# - JWT_SECRET
